import { translator, type Locale } from '@/lib/i18n';
import Link from '@/components/locale-link';
import { Breadcrumb } from '@/components/shared';
import { pageMetadata } from '@/lib/metadata';
export const metadata = pageMetadata('Privacy & data', 'How saved projects, inquiry drafts and career profiles are handled on the NORTHFORGE website.', '/privacy/');
export default function PrivacyPage({ locale = 'en' }: { locale?: Locale }) {
  const t = translator(locale); return <div className="wrap information-page"><Breadcrumb items={[{ label: 'Privacy' }]} /><div className="page-heading"><h1>{t("Your information.")}<br /> {t(" Under your control.")}</h1></div><div className="prose privacy-prose"><section><h2>{t("What the website saves.")}</h2><p>{t("When you save a project, prepare an inquiry or create a career profile, the information is kept in your current browser using local storage. This can include project references, project descriptions, your name, email address and any notes you choose to add.")}</p></section><section><h2>{t("How that information is used.")}</h2><p>{t("The website uses these records to restore your drafts and saved projects when you return on the same browser and device. Saving an inquiry or career profile does not transmit it to a team or external form service. You can download a copy when you want to share it yourself.")}</p><p>{t("Private details are not added to page URLs. Sharing a project link shares the public case study; sharing the contact-page URL does not share your local inquiry.")}</p></section><section><h2>{t("Retention and removal.")}</h2><p>{t("Your records remain in browser storage until you remove them or clear your browser data. You can edit, download or remove individual inquiries, and reset all NORTHFORGE data through ")}<Link href="/website-information/">{t("Website information")}</Link>{t(". The reset only removes this website’s saved data.")}</p><p>{t("If storage is unavailable, the website keeps changes in memory for the current visit and displays a notice. These changes will be lost when the page is reloaded or closed.")}</p></section><section><h2>{t("Cookies and hosting.")}</h2><p>{t("This website does not add analytics, advertising trackers or tracking cookies. Its images, fonts and public project content are bundled with the site.")}</p><p>{t("The hosting provider processes requests needed to deliver and secure the website, which may include IP addresses and technical request logs. Access to a private hosted site may also require the hosting provider’s sign-in. Those services are separate from the browser records described here.")}</p></section><section><h2>{t("Shared devices.")}</h2><p>{t("Anyone using the same browser profile may be able to see saved information. Download and remove records when you are finished on a shared device. Saved data does not synchronise between devices.")}</p></section></div></div>; }
