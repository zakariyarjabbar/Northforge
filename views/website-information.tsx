import { translator, type Locale } from '@/lib/i18n';
import { Breadcrumb } from '@/components/shared';
import { DataControls } from '@/components/data-controls';
import { pageMetadata } from '@/lib/metadata';
export const metadata = pageMetadata('Website information', 'Accessibility, image credits and browser data controls for the NORTHFORGE website.', '/website-information/');
export default function WebsiteInformation({ locale = 'en' }: { locale?: Locale }) {
  const t = translator(locale); return <div className="wrap information-page"><Breadcrumb items={[{ label: 'Website information' }]} /><div className="page-heading"><h1>{t("Website information.")}</h1></div><div className="information-layout"><div className="prose"><section><h2>{t("Designed to be useful.")}</h2><p>{t("Browse projects by expertise and region, save work that interests you and prepare a project inquiry. Each case study can be printed as a clean project summary.")}</p></section><section><h2>{t("Accessibility.")}</h2><p>{t("The website supports keyboard navigation, text enlargement and reduced motion preferences. The project atlas includes a project list with the same locations and links. The main content is available without motion or map interaction.")}</p></section><section><h2>{t("Images and credits.")}</h2><p>{t("Project imagery was commissioned for this website and created with OpenAI ImageGen. The world map uses public-domain Natural Earth geographic data. Manrope and Noto Sans Arabic are used under the SIL Open Font License.")}</p><p>{t("Project positions identify their regional setting rather than a site boundary.")}</p></section><section><h2>{t("Your saved information.")}</h2><p>{t("Saved projects, inquiry records and career profiles are stored on this device. Clearing your browser data removes them. Information is not synchronised between browsers or devices.")}</p></section></div><DataControls/></div></div>; }
