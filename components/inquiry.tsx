'use client';
import { useState } from 'react';
import { useSearchParams } from 'next/navigation';
import Link from 'next/link';
import { expertise, expertiseById, projects, regionById, regions } from '@/lib/content';
import { clearDraft, downloadText, removeInquiry, saveDraft, saveInquiry, useWorkspace, type Inquiry, type InquiryDraft } from '@/lib/storage';
import { Arrow, Check, Close } from './icons';
const empty: InquiryDraft = { expertise: '', region: '', description: '', schedule: '', name: '', email: '', references: [] };
function validate(value: InquiryDraft, step: number) {
  const errors: Record<string,string> = {};
  if (step === 0 || step === 2) { if (!value.expertise) errors.expertise = 'Choose the expertise your project needs.'; if (!value.region) errors.region = 'Choose the region for your project.'; if (value.description.trim().length < 20) errors.description = 'Add a little more detail (at least 20 characters).'; }
  if (step === 1 || step === 2) { if (value.name.trim().length < 2) errors.name = 'Enter your name.'; if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value.email.trim())) errors.email = 'Enter a valid email address.'; }
  return errors;
}
function FieldError({ id, errors }: { id: string; errors: Record<string,string> }) { return errors[id] ? <p className="field-error" id={`${id}-error`}>{errors[id]}</p> : null; }
function InquiryDetails({ value }: { value: InquiryDraft }) { return <dl className="inquiry-details"><div><dt>Expertise</dt><dd>{expertiseById(value.expertise)?.name}</dd></div><div><dt>Region</dt><dd>{regionById(value.region)?.name}</dd></div><div className="full"><dt>Project brief</dt><dd className="preserve-lines">{value.description}</dd></div>{value.schedule && <div className="full"><dt>Schedule / stage</dt><dd>{value.schedule}</dd></div>}<div><dt>Name</dt><dd>{value.name}</dd></div><div><dt>Email</dt><dd>{value.email}</dd></div>{value.references.length > 0 && <div className="full"><dt>Project references</dt><dd>{value.references.map(id => projects.find(p => p.id === id)?.name).filter(Boolean).join(', ')}</dd></div>}</dl>; }
function inquiryText(record: Inquiry) { return `NORTHFORGE GROUP\nPROJECT INQUIRY ${record.id}\n\nSaved: ${record.updatedAt}\nRegional team: ${regionById(record.region)?.team}\nExpertise: ${expertiseById(record.expertise)?.name}\nRegion: ${regionById(record.region)?.name}\n\nPROJECT BRIEF\n${record.description}\n\nSchedule / stage: ${record.schedule || 'Not specified'}\n\nName: ${record.name}\nEmail: ${record.email}\n\nReferences:\n${record.references.map(id => projects.find(p => p.id === id)?.name).filter(Boolean).join('\n') || 'None selected'}\n\nSaved in this browser.\n`; }
export function InquiryFlow() {
  const params = useSearchParams(); const state = useWorkspace();
  const defaults: InquiryDraft = { ...empty, expertise: expertise.some(e => e.id === params.get('expertise')) ? params.get('expertise')! : '', region: regions.some(r => r.id === params.get('region')) ? params.get('region')! : '', references: state.saved };
  const form = state.draft ?? defaults;
  const availableReferences = [...new Set([...state.saved, ...form.references])];
  const [step,setStep] = useState(0); const [errors,setErrors] = useState<Record<string,string>>({}); const [recordId,setRecordId] = useState<string|null>(null); const [editId,setEditId] = useState<string|undefined>(); const [confirmDelete,setConfirmDelete] = useState(false);
  const record = state.inquiries.find(i => i.id === recordId);
  const change = (key: keyof InquiryDraft, value: string|string[]) => { saveDraft({ ...form, [key]: value }); if (errors[key]) setErrors(current => { const next = { ...current }; delete next[key]; return next; }); };
  function advance() { const found = validate(form,step); setErrors(found); if (Object.keys(found).length) { document.getElementById(Object.keys(found)[0])?.focus(); return; } if (step < 2) { saveDraft(form); setStep(step+1); document.getElementById('inquiry-heading')?.focus(); } else { const saved = saveInquiry({ ...form, name: form.name.trim(), email: form.email.trim(), description: form.description.trim() },editId); setRecordId(saved.id); setEditId(undefined); setConfirmDelete(false); } }
  function edit(value: Inquiry) { saveDraft(value); setEditId(value.id); setRecordId(null); setStep(0); setErrors({}); setConfirmDelete(false); }
  function startFresh() { clearDraft(); setRecordId(null); setEditId(undefined); setStep(0); setErrors({}); }
  if (record) return <div className="inquiry-completion print-summary"><div className="success-mark"><Check size={30}/></div><p className="reference-number">{record.id}</p><h2>Your project brief is ready.</h2><p className="completion-intro">Your inquiry is saved. Review the details or download a copy to share.</p><div className="routing-label">{regionById(record.region)?.team}</div><InquiryDetails value={record}/><div className="record-actions print-control"><button className="button" onClick={() => downloadText(`${record.id}.txt`,inquiryText(record))}>Download summary <Arrow size={18}/></button><button className="button button-outline" onClick={() => window.print()}>Print</button><button className="button button-outline" onClick={() => edit(record)}>Edit inquiry</button></div><div className="minor-actions print-control"><button onClick={() => downloadText(`${record.id}.json`,JSON.stringify(record,null,2),'application/json')}>Download JSON</button><button onClick={() => setConfirmDelete(true)}>Remove inquiry</button><button onClick={startFresh}>Create another inquiry</button></div>{confirmDelete && <div className="inline-confirm print-control" role="group" aria-label="Confirm inquiry removal"><p>Remove {record.id}? This cannot be undone.</p><button className="button" onClick={() => { removeInquiry(record.id); startFresh(); }}>Remove inquiry</button><button className="button button-outline" onClick={() => setConfirmDelete(false)}>Keep inquiry</button></div>}</div>;
  return <div className="inquiry-workspace"><ol className="form-steps" aria-label="Inquiry progress">{['Your project','Your details','Review'].map((label,i) => <li key={label} aria-current={step===i ? 'step' : undefined}><span>{step>i ? <Check size={15}/> : i+1}</span>{label}</li>)}</ol><form noValidate onSubmit={e => { e.preventDefault(); advance(); }}><h2 id="inquiry-heading" tabIndex={-1}>{step===0 ? 'Tell us what you have in mind.' : step===1 ? 'Add your details.' : 'Everything in place?'}</h2><p className="form-intro">{step===0 ? 'A few details help put your project in context.' : step===1 ? 'Include a contact for the project brief.' : 'Review your brief before saving your inquiry.'}</p>{Object.keys(errors).length>0 && <div className="error-summary" role="alert">Please check the highlighted fields.</div>}{step===0 && <><div className="form-grid"><div className="field"><label htmlFor="expertise">Expertise <span aria-hidden="true">*</span></label><select id="expertise" value={form.expertise} onChange={e => change('expertise',e.target.value)} aria-invalid={!!errors.expertise} aria-describedby={errors.expertise ? 'expertise-error' : undefined} required><option value="">Select expertise</option>{expertise.map(e => <option key={e.id} value={e.id}>{e.name}</option>)}</select><FieldError id="expertise" errors={errors}/></div><div className="field"><label htmlFor="region">Region <span aria-hidden="true">*</span></label><select id="region" value={form.region} onChange={e => change('region',e.target.value)} aria-invalid={!!errors.region} aria-describedby={errors.region ? 'region-error' : undefined} required><option value="">Select region</option>{regions.map(r => <option key={r.id} value={r.id}>{r.name}</option>)}</select><FieldError id="region" errors={errors}/></div><div className="field full"><label htmlFor="description">Your project <span aria-hidden="true">*</span></label><textarea id="description" value={form.description} onChange={e => change('description',e.target.value)} rows={5} maxLength={5000} placeholder="The challenge, the location, and what you want to achieve." aria-invalid={!!errors.description} aria-describedby={errors.description ? 'description-error' : undefined} required/><FieldError id="description" errors={errors}/></div><div className="field full"><label htmlFor="schedule">Schedule or project stage <span>(optional)</span></label><input id="schedule" value={form.schedule} onChange={e => change('schedule',e.target.value)} maxLength={200} placeholder="For example, feasibility study or planned start date"/></div></div>{availableReferences.length>0 && <fieldset className="reference-picker"><legend>Include saved project references <span>(optional)</span></legend>{availableReferences.map(id => <label key={id}><input type="checkbox" checked={form.references.includes(id)} onChange={e => change('references',e.target.checked ? [...form.references,id] : form.references.filter(x=>x!==id))}/>{projects.find(p=>p.id===id)?.name}</label>)}</fieldset>}</>}{step===1 && <div className="form-grid"><div className="field full"><label htmlFor="name">Your name <span aria-hidden="true">*</span></label><input id="name" autoComplete="name" value={form.name} onChange={e => change('name',e.target.value)} maxLength={160} aria-invalid={!!errors.name} aria-describedby={errors.name ? 'name-error' : undefined} required/><FieldError id="name" errors={errors}/></div><div className="field full"><label htmlFor="email">Email address <span aria-hidden="true">*</span></label><input id="email" type="email" autoComplete="email" value={form.email} onChange={e => change('email',e.target.value)} maxLength={254} aria-invalid={!!errors.email} aria-describedby={errors.email ? 'email-error' : undefined} required/><FieldError id="email" errors={errors}/></div></div>}{step===2 && <><div className="routing-label">{regionById(form.region)?.team}</div><InquiryDetails value={form}/><p className="data-note">Your inquiry stays in this browser. Download a copy to share it. <Link href="/privacy/">Privacy & data</Link></p></>}<div className="form-actions">{step>0 && <button className="button button-outline" type="button" onClick={() => { setStep(step-1); setErrors({}); }}>Back</button>}<button className="button" type="submit">{step===2 ? editId ? 'Save changes' : 'Save inquiry' : step===1 ? 'Review inquiry' : 'Continue'}<Arrow size={18}/></button><span>{state.draft ? state.persistence ? 'Draft saved' : 'Draft kept for this visit' : 'Required fields marked *'}</span></div></form>{state.inquiries.length>0 && <section className="previous-inquiries"><h3>Your saved inquiries</h3>{state.inquiries.slice().reverse().map(i => <button key={i.id} onClick={() => { setRecordId(i.id); setConfirmDelete(false); }}><span>{i.id}<small>{expertiseById(i.expertise)?.short} · {regionById(i.region)?.name}</small></span><Arrow size={18}/></button>)}</section>}{state.draft && <button className="discard-draft" onClick={() => { if(window.confirm('Discard this inquiry draft?')) startFresh(); }}>Discard draft <Close size={14}/></button>}</div>;
}
