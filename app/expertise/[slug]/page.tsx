import { notFound } from 'next/navigation';
import { expertise, projects } from '@/lib/content';
import { Breadcrumb, ContactBand, Photo, ProjectCard } from '@/components/shared';
import { pageMetadata } from '@/lib/metadata';
export function generateStaticParams() { return expertise.map(e => ({ slug: e.id })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const e = expertise.find(e => e.id === slug); return e ? pageMetadata(e.name, e.description, `/expertise/${e.id}/`, `/og/${e.image}.jpg`) : {}; }
export default async function ExpertiseDetail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const e = expertise.find(x => x.id === slug); if (!e) notFound(); return <><div className="wrap"><Breadcrumb items={[{ label: 'Expertise', href: '/expertise/' }, { label: e.name }]} /><div className="page-heading"><h1>{e.title}</h1><p>{e.description}</p></div></div><div className="expertise-detail-hero"><Photo id={e.image} alt={e.description} priority /><span>{e.name}</span></div><div className="wrap"><section className="capability-intro"><h2>{e.name}</h2><div><p>{e.intro}</p><h3>Our scope</h3><ul className="scope-list">{e.scopes.map(s => <li key={s}>{s}</li>)}</ul></div></section><section className="capability-approach"><h2>A connected approach.</h2><ol>{e.approach.map((a,i) => <li key={a}><span>{String(i+1).padStart(2,'0')}</span><h3>{a}</h3></li>)}</ol></section><section className="related-projects"><div className="section-heading"><h2>{e.short} in practice.</h2></div><div className="project-grid">{projects.filter(p => p.expertise === e.id).map(p => <ProjectCard project={p} key={p.id} />)}</div></section></div><ContactBand discipline={e.id} /></>; }
